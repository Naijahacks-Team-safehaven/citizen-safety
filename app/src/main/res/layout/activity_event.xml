<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EventActivity">

    <TextView
        android:id="@+id/contact_personnel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="100dp"
        android:text="Contact"
        android:layout_marginStart="@dimen/fab_margin"
        android:layout_marginLeft="@dimen/fab_margin"
        android:textSize="@dimen/fifteen_sp_sp"
        android:textStyle="bold">
    </TextView>

    <TextView
        android:id="@+id/label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/contact_personnel"
        app:layout_constraintTop_toBottomOf="@+id/contact_personnel"
        android:layout_marginTop="30dp"
        android:text="Enter brief description of the situation">
    </TextView>

    <EditText
        android:id="@+id/event_box"
        android:layout_width="match_parent"
        android:layout_height="170dp"
        android:ems="10"
        android:gravity="start|top"
        android:inputType="textMultiLine"
        tools:layout_editor_absoluteX="100dp"
        app:layout_constraintTop_toBottomOf="@id/label"
        app:layout_constraintStart_toStartOf="@id/label"
        android:background="@drawable/input_bg"
        android:layout_margin="@dimen/fab_margin"/>

   <TextView
       android:id="@+id/enable_location"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       app:layout_constraintStart_toStartOf="@+id/event_box"
       app:layout_constraintTop_toBottomOf="@+id/event_box"
       android:layout_marginTop="50dp"
       android:text="Use GPS location">
   </TextView>
    <Switch
        android:id="@+id/location_swith"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:layout_constraintTop_toTopOf="@id/enable_location"
        app:layout_constraintBottom_toBottomOf="@id/enable_location"
        app:layout_constraintEnd_toEndOf="parent"
        android:checked="true"
        android:layout_marginRight="@dimen/fab_margin">
    </Switch>

    <Button
        android:id="@+id/send_button"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/enable_location"
        android:background="@color/colorPrimary"
        android:text="SEND"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/fifty_dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
    </Button>
</androidx.constraintlayout.widget.ConstraintLayout>